name: 'pipeline_bancaire'
version: '1.0.0'
config-version: 2

profile: 'pipeline_bancaire'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

models:
  pipeline_bancaire:
    +location: europe-west1
    staging:
      +materialized: view
    mart:
      +materialized: table